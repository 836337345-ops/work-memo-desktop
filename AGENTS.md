# 工作备忘录协作约定

默认简体中文。已获用户授权实施首期、本地 Git 提交和集成，禁止上传、发布或远程推送。
每位开发员只在自己的工作树和指定分支修改文件，不切换或写入其他开发员工作区。
编辑使用 apply_patch。任务交付必须提交代码，并提供提交编号、验证结果与遗留问题。
公共契约以 src/types.ts、src/api.ts、docs/CONTRACT.md 为准；变更先报告 PM，不自行改变契约。
真实数据不得用于测试。用户数据位于应用数据目录，与源码和安装目录隔离。
首期不做文字汇总、AI、云同步、定时提醒或永久删除。
