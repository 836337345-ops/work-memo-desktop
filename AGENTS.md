# 工作备忘录项目协作约定

默认使用简体中文，面向非技术用户先说明功能结果，再说明必要原因。禁止上传、发布、远程推送或读取真实用户数据。

## 角色边界

- 当前对话为项目总 PM 时，只负责需求确认、任务卡、进度协调、公共契约、提交审查、集成和最终验收。
- 业务功能必须分发给用户可见的独立 Codex 开发任务；不得用隐藏子任务替代，也不得由 PM 悄悄完成整张开发任务卡。
- 每名开发员使用独立工作树和分支，只修改任务卡允许的范围。公共接口变更必须先报告 PM。
- 普通功能、数据安全和发布任务由独立测试员验收；纯文案、颜色或单组件布局微调默认由开发员自测、PM 审查，不单开测试任务。
- 测试员只改测试和验收报告，不替开发员修业务代码；缺陷退回原开发员，修复后在固定集成提交上复测。
- PM 仅可直接处理公共契约、集成冲突和必要的集成胶水；若涉及可见业务行为，应重新派给开发员。
- PM 可以查看代码、审查差异、集成提交、运行测试和更新协作文档。准备修改业务文件前，必须先确认该修改是否属于开发任务卡；任何改变用户可见行为的代码都必须退回开发员完成。

## 任务分级与模型

- 微调只创建一个可见开发任务，完成相关测试、构建和 PM 审查；普通功能、数据安全和发布采用开发、集成、独立测试的完整流程。具体边界见 `docs/PROJECT-COLLABORATION.md`。
- PM 默认使用 Sol Light；确需多步判断时使用 Medium。High、Extra High、Max 或 Ultra 仅在用户明确同意时使用。
- 开发和独立测试默认使用 Terra Medium；Luna 只用于机械性文档整理和无业务判断的只读盘点。
- 每个版本只保留一个活跃 PM。旧 PM 接管后只读归档，不继续并行指挥。

## 任务分发验真

PM 只有同时确认以下事项，才可以宣布任务已经分发并开始开发：

1. 已生成可继续发送消息的正式任务 ID，不只停留在临时任务编号。
2. 独立工作树真实存在，分支和固定基线正确。
3. 工作树依赖脚本执行成功，前端依赖和 Rust 缓存可用。
4. 开发员已经发出第一条进度，并实际开始检查或修改。
5. `docs/ACTIVE-VERSION.md` 已记录任务卡、窗口、任务 ID、工作树、分支、基线和状态。

若任一项失败，任务状态标记为“未启动”或“环境阻断”，不得用“已分发”代替。

## 版本阶段锁

每个版本只能处于一个阶段：需求整理 → 用户确认 → 开发中 → PM 集成 → 独立测试 → 用户调试验收 → 等待打包授权 → 已交付。

- 需求未确认不得派发开发；开发未集成不得开始最终测试。
- 独立测试期间不得向固定测试提交混入其他功能。
- 测试失败必须退回原开发员，修复集成后重新固定提交复测。
- 微调不单开 QA 时，独立测试阶段记为“不适用”，并记录开发自测与 PM 审查结果。
- 用户未完成调试验收不得进入打包；未明确说“确认通过，可以打包”不得处理安装包或 `main`。

## 每轮开发流程

1. PM 先梳理需求、未决问题和验收标准，获得用户确认后建立版本基线。
2. PM 创建任务卡，写明负责人、分支、允许修改范围、依赖接口、验收条件、禁止事项和停止条件。
3. 开发员先运行 `powershell.exe -NoProfile -NonInteractive -ExecutionPolicy Bypass -File .\scripts\setup-codex-worktree.ps1`，再复核现有实现，只修本卡范围。
4. 开发员在安全的小阶段及时提交；完成时工作区必须干净，并回报提交号、测试结果和遗留问题。
5. PM 审查后集成到 `integration/v2` 或下一条明确的集成分支；只在固定集成提交上执行一次与风险相称的版本检查，不重复开发员和测试员已经提供的同类验证。
6. 普通功能、数据安全和发布任务由测试员在唯一固定提交上独立测试。失败则退回开发员，修复后重新固定提交复测。
7. 调试程序只做短时启动验证；记录提交、隔离数据目录和结果后结束任务，不让 PM 回合长驻等待用户操作。
8. 自动测试通过后供用户验收。只有用户明确说“确认通过，可以打包”时，才允许生成安装包或合入 `main`。

## 数据、Git 与交付

- 编辑文件使用 `apply_patch`。保留用户已有修改，不执行破坏性 Git 操作。
- 公共契约以 `src/types.ts`、`src/api.ts`、`docs/CONTRACT.md` 为准。
- 测试只使用隔离的虚构数据；应用数据位于用户应用数据目录，与源码和安装目录分离。
- 不自动删除工作树、分支、备份或用户数据。清理前必须确认提交已集成、工作区无未提交内容，并取得用户确认。
- 创建新工作树前检查 C 盘和共享缓存所在磁盘的可用空间；空间不足时停止创建并报告，不重复安装依赖。
- 微调执行相关测试和 `npm run build`；普通功能及更高风险改动执行 `npm test` 和 `npm run build`；数据层改动再执行 `cargo test --manifest-path src-tauri/Cargo.toml`。
- 普通功能及更高风险版本结束后更新交接快照和 QA 报告；新 PM 对话优先读取快照，不依赖完整聊天历史。

## 中断恢复与完成定义

- 每个任务在 `docs/ACTIVE-VERSION.md` 记录“已完成、正在做、下一步、未提交文件”。额度、程序或电脑中断后，PM 必须先读取该记录和 Git 状态，再从中断点续派，不得直接建立重复任务。
- 旧窗口无法恢复时，新窗口必须先接管并核对旧工作树、分支和未提交修改。
- 一个版本只有在任务分级要求的开发、集成、检查、测试和用户验收状态均已记录后，才能标记完成；不适用或无法执行的检查必须如实记录。

完整流程见 `docs/PROJECT-COLLABORATION.md`，当前项目事实见 `docs/PROJECT-HANDOFF.md`，实时任务状态见 `docs/ACTIVE-VERSION.md`；当前版本过程见 `docs/HANDOFF-V2.9.md`，额度审计见 `docs/HANDOFF-SOL-USAGE-20260909.json`。
