# V2.4 独立验收报告

## 被测版本与测试边界

- 固定被测集成提交：`5975318`（`fix: align work calendar status colors`）。
- 本轮只变更 `tests/**` 和本报告，未改动业务代码、未连接远程、未生成安装包。
- 测试数据均为虚构的地产推广事项。浏览器验收使用内存 IPC 双替身，数据路径固定为 `C:/qa-isolated`，不会读取、写入或恢复真实用户数据。
- 前端构建产物、浏览器报告、截图和跟踪文件均写入 D 盘临时 QA 目录；Rust 无改动，未重复执行重型 Rust 构建。

## 验收结果

| 编号 | 验收点 | 验证方式 | 结果 |
| --- | --- | --- | --- |
| V24-01 | 左侧“工作日历”入口及激活状态；进入后隐藏右侧详情栏 | Edge UI/IPC 模拟 | 通过 |
| V24-02 | 默认当月、周一首日、固定 42 个日期格；上月、回到今天、下月导航 | WorkCalendar 单测 + Edge UI/IPC 模拟 | 通过 |
| V24-03 | 有截止日期且未删除事项显示；四种状态标记颜色与事项卡一致（进行中绿、待开展灰、已暂停红、已完成橙） | WorkCalendar 单测 + Edge CSS 计算样式核验 | 通过 |
| V24-04 | 鼠标悬停及键盘聚焦日期时显示该日完整事项标题列表 | WorkCalendar 单测 + Edge 键盘聚焦/浮层核验 | 通过 |
| V24-05 | 日历包含全部未删除且有日期事项，不受搜索、状态、时间、分类筛选影响；无日期和回收站事项不显示 | Edge UI/IPC 模拟，含组合筛选和虚构排除数据 | 通过 |
| V24-06 | 日历只读，不能编辑事项 | Edge UI/IPC 模拟：无新建入口、无输入框/文本框/状态选择框 | 通过 |
| V24-07 | 关闭日历、显示全部、筛选、回收站等入口可正确回到列表 | Edge UI/IPC 模拟 | 通过 |
| V24-08 | “情况”独立底框；最新进度、情况、跟进清单、备注标题字号大于内容；最新进度完整展示 | ItemList 单测 + Edge CSS/UI 核验 | 通过 |
| V24-09 | 1060×700 及常用尺寸底部可访问，无新增双滚动条或内容遮挡 | Edge 1060×700 日历滚动核验；既有 1060×360 列表/详情回归 | 通过 |
| V24-10 | V2.3 核心流程：新增/编辑、进度更新、跟进清单、筛选、备份恢复与 TXT 导出 | 单元/契约测试 + Edge UI/IPC 模拟回归 | 通过 |

## 执行记录

- `npm run build -- --outDir D:\qa-v24-dist`：通过。
- `npm test`：通过，10 个文件、47 项测试。
- `QA_UI_READY=1 npm run test:e2e -- --output D:\qa-v24-test-results-full`：通过，Edge 15 项流程全部通过。

## 缺陷与无法验证项

本轮未发现产品缺陷，因此无缺陷复现步骤。

以下项目属于浏览器 IPC 模拟的边界，标记为**无法验证**，并非失败：真实 SQLite 持久化、真实 Windows 文件选择/确认窗口、真实 Tauri 桌面窗口缩放与重开、系统字体及原生键盘辅助功能。PM 如需最终人工确认，应使用全新的隔离 `WORK_MEMO_DATA_DIR` 启动调试应用；不得使用真实用户数据。
