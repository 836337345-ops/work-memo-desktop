# V2.11 独立验收报告

- 验收日期：2026年9月10日
- 验收人员：独立测试员（T112）
- 固定验收提交：`900e80dff7b1ac658defd8d1339fbefbf2d36df5`
- 验收分支：`codex/v211-independent-qa`
- 数据边界：未启动桌面程序，未读取正式用户数据，未执行真实 Windows 开机自启动的开启或关闭。

## 结论

通过。固定提交满足任务卡的自动化、构建、Rust 测试和最小权限要求，未发现可稳定复现的业务缺陷。

## 功能与安全复核

1. 左侧栏底部在“导出事项”之后提供名称为“开机自启动”的可访问开关；读取或切换期间禁用，错误信息就近以提示区域展示。
2. 前端初始化调用官方 `isEnabled()` 读取状态；切换调用 `enable()` 或 `disable()` 后再次读取真实状态。读取或切换失败时保留原界面状态并显示中文提示；忙碌状态阻止连续点击。
3. 仅新增官方 `@tauri-apps/plugin-autostart` / `tauri-plugin-autostart` v2 依赖；Rust 启动器已注册。能力配置只新增 `autostart:allow-enable`、`autostart:allow-disable` 和 `autostart:allow-is-enabled`，未发现注册表脚本、计划任务、管理员权限或无关权限。
4. 固定提交的业务改动限于自启动依赖、注册、权限、界面及其测试；现有事项、日历、跟进、备份和导出测试均随完整前端及 Rust 测试通过。

## 自动化与构建

| 检查 | 结果 |
| --- | --- |
| `npm test` | 14 个测试文件、101 项通过，0 项失败 |
| `npm run build` | 通过 |
| `cargo test --manifest-path src-tauri/Cargo.toml` | 14 项通过，0 项失败 |
| `git diff --check 13b3b87..900e80d` | 通过 |

## 未覆盖范围

依照任务安全边界，未调用真实 Windows 启动项的开启或关闭接口，因而没有对实际登录后启动窗口进行人工验证。该验证只能在隔离调试标识下、记录并恢复启动项状态后另行进行；不得触碰正式安装版启动项。
