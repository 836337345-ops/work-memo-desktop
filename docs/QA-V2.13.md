# V2.13 独立测试报告

- 测试员：T132
- 最近固定被测提交：`f0e009694d1461686aaa6eb7c9dd4be9b56aed02`
- 测试基线：`637ccd1474cce9ca5f05320f624a7c705ee7fc2c`
- 环境：`scripts/setup-codex-worktree.ps1` 成功；使用隔离虚构数据、前端依赖与 Rust 缓存。
- 结论：**通过。**

## 复测结果

- 原阻断缺陷已修复：从日历新建、仅保留预填日期时直接取消，不弹放弃确认；实际填写内容后只确认一次。
- 回收站：单选、搜索结果范围、取消确认和确认删除均通过；确认后只删除勾选的回收站事项，未选事项保留。后端测试确认正常事项或混合目标会被拒绝且不会部分删除，并确认关联跟进、进度会随永久删除移除。
- 完成事项保存时会强制取消星标；事项卡与日历均不展示完成事项星标，完成状态不提供重要事项操作。切回进行中或暂停后可重新设置星标，但不会自动恢复。
- 左栏：已验证“工作列表”“进度/全部进度”文案、日历/时间/分类等既有入口，以及分类新增对话框、行内改名、删除取消和稳定 ID 拖拽排序。开发提交曾缩减 Sidebar 旧回归覆盖，QA 已补足未改动入口、筛选、展开状态和自启动入口的回归保护。

## 自动检查

- `npm test`：14 个测试文件、117 项通过。
- `npm run build`：通过。
- `cargo test --manifest-path src-tauri/Cargo.toml`：18 项通过。
- `git diff --check`：通过。

## 未测边界

- Tauri 原生确认框以自动化 mock 验证调用、取消和确认逻辑；本机桌面自动化未连接到隔离 Tauri 窗口，未实际点击操作系统原生对话框。
- 浏览器模拟覆盖了拖拽事件、插入预览和排序提交，但未在真实桌面窗口以鼠标完成一次拖放；应在用户调试验收时补充该边界。

## 70e22ca 分类交互复测

- 业务差异仅为 Sidebar 组件、样式和对应测试；新增分类入口只打开模态对话框，列表中不保留常驻输入框。取消清空草稿，成功关闭，失败保留输入并显示中文错误。
- 每行拖拽把手位于名称左侧，改名和删除操作位于右侧；分别验证了蓝色改名类和红色删除类、筛选/改名/删除不会触发排序。
- 向上、向下拖拽均按实际落点显示插入预览并提交稳定 ID 顺序；无落点、原位释放不提交，保存失败保持原顺序并提示错误。

## f0e0096 Pointer 拖拽与资源复测

- 已确认不再使用 HTML5 `drag` 事件；把手使用 pointer down/move/up、pointer capture 和 cancel 清理。向上与向下移动的插入线、最终稳定 ID 顺序和单次提交均通过自动化验证；取消、原位释放、失败恢复及分类筛选/改/× 不误触排序均通过。
- 删除操作显示“×”，并提供“删除 <分类名>”无障碍名称；确认文案及删除语义未变。改名为蓝色“改”，仍支持行内编辑。
- `src/assets/category-drag-handle.png` 为项目内实际导入的 1254×1254 32 位 ARGB PNG，检测到透明像素；生产构建也输出该资源。
- 数据层本轮未变，复用前次 `cargo test` 的 18/18 通过结论；本轮重新执行前端完整测试、构建和差异检查。
