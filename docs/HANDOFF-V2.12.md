# V2.12 精简交接

最后更新：2026年9月13日

## 结论与代码锚点

- V2.12 已交付，产品版本 `0.1.1`；需求以 `docs/TASK-V2.12.md` 为准。
- 固定业务基线 `e01e361`；任务起始提交 `fafc0ba`；最终业务提交 `a01e137`；T128 固定测试提交 `43d090d`（对应业务提交 `a01e137`）；QA 证据集成 `a3dd276`；交接整理前当前集成提交 `637ccd1`。
- 这些提交含义不同。`43d090d` 与 `637ccd1` 的文档、测试和发布记录差异不构成业务基线冲突。

## 有效任务与证据

- 唯一开发任务：T121/T124/T127 共用 `01a09383-267f-7860-9d30-e28744c98373`；工作树 `C:/Users/Administrator/.codex/worktrees/5ead/工作备忘录`；分支 `codex/v212-calendar-star-followup`。
- 唯一 QA：T128 `01a094f2-7711-7670-8f4f-fb69f3ccce01`；工作树 `C:/Users/Administrator/.codex/worktrees/e9df/工作备忘录`；分支 `codex/v212-second-acceptance-qa`；报告 `docs/QA-V2.12-SECOND-ACCEPTANCE.md`。
- 早期证据：首次 QA 见 `docs/QA-V2.12.md`；第一次验收修复 QA 见 `docs/QA-V2.12-ACCEPTANCE.md`。T122、T125、T126 仅作历史索引，不再使用。
- T125 因沿用旧“默认进行中列表”口径而失败；V2.10 起正确契约为启动默认进入工作日历。T126 后续通过，但不再作为当前 QA 窗口。
- T128 创建回调延迟期间产生的重复 QA 任务 `01a094f3-1a2a-7291-ada1-e86af7593c66`、`01a094f3-c219-7f93-8f50-8973374ea0ba` 已停用，禁止续派。

## 验证、实测与交付

- T128 定向测试 `40/40` 通过；复用开发证据：完整前端 `112/112`、构建通过；Rust 在第二轮未改，复用本版此前 `15/15` 结果。测试数据均隔离且为虚构数据。
- T128 自动化环境没有取得桌面窗口，未能真实点击 Tauri 原生删除确认框；这是 QA 工具覆盖限制。用户随后已真实操作“取消”和“确认删除”两条路径并确认功能实现，因此用户调试验收完成，不能再写成待用户验收。
- 安装包：`src-tauri/target/release/bundle/nsis/工作备忘录_0.1.1_x64-setup.exe`；构建来源 `4dd2bba`；SHA-256 `738B0B789B332D0D24FE81645063143003F5DDC8AE88744D233BE88B83640674`；未签名。
- `main` 与 `integration/v2` 已合入并同步远程。V2.12 的打包依据是用户对 V2.12 的单独明确授权，不沿用 V2.11 授权。

## 未提交修改与保留边界

- 主工作树本轮只修改七个协作文档，用于第四任 PM 接管；按用户要求不提交、不推送。
- 工作树 `3952` 有未提交 `docs/ACTIVE-VERSION.md`，用途为旧 QA 协作记录；工作树 `f042` 有未提交 `ExportPanel.tsx` 与对应测试，属于历史业务修改。交接未读取内容，不判断可用性，不得自动覆盖或清理。
- 其他已登记工作树在本轮核对时干净；所有工作树、分支、窗口和数据均保留。

## 后续省额度规则

- 默认一个开发任务和一个独立 QA；微调不单开 QA。缺陷修复与复测更新固定提交并继续原窗口。
- 创建请求发出后保持“创建中”；回调延迟、额度耗尽、中断或暂时无法查询均不能触发重复创建。无查询工具时报告限制，不猜测任务不存在。
- 只有确认原任务不可恢复、记录证据且确认不会继续执行后，才能安排一个替代任务。文档口径冲突先同步用户已确认需求与契约，再由原 QA 复测。
- 新需求按风险和依赖合并成版本任务，不按按钮拆卡。微调做定向验证；保存、删除、迁移等行为保留相称的独立验证。
- PM 只审查差异、集成风险和可信证据；相同提交不重复全测。开发与 QA 只读本任务必需资料。
- PM 默认 Sol Light，复杂判断临时 Medium；开发和 QA 默认 Terra Medium。实际档位无法核实时如实说明，提示词不等于设置已生效。

## 历史入口

- 稳定产品事实：`docs/PROJECT-HANDOFF.md`；协作流程：`AGENTS.md`、`docs/PROJECT-COLLABORATION.md`。
- 历史版本按需查看对应 `docs/HANDOFF-V*.md`、`docs/TASK-V*.md` 与 `docs/QA-V*.md`；额度审计 `docs/HANDOFF-SOL-USAGE-20260909.json` 仅在用量或重复任务问题复盘时读取。
