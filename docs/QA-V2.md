# V2 独立验收报告

## 被测范围

最终被测集成提交：`261cfa7`（`merge: guard v2 inline saves before navigation`）。本候选在原 V2 版本上加入内联保存离开保护。本报告的测试数据均为虚构的地产推广事项；浏览器测试只在内存中模拟，不读取、写入或恢复真实用户数据。

| 编号 | 验收点 | 验证方式 | 结果 |
| --- | --- | --- | --- |
| V2-01 | 左栏顺序为状态、时间、分类 | Edge UI/IPC 模拟 | 通过 |
| V2-02 | 状态、时间、分类仅保留一个生效维度，搜索可叠加 | Edge UI/IPC 模拟 | 通过 |
| V2-03 | 历史包含所有状态，逾期仅包含待开展/进行中 | Edge UI/IPC 模拟 + 筛选单测 | 通过 |
| V2-04 | 卡片展示标题、情况、最新进度、跟进、备注、状态、类别和日期 | Edge UI/IPC 模拟 + 组件单测 | 通过 |
| V2-05 | 卡片内可新增进度、编辑备注/跟进/情况/状态、一键完成；类别日期需进入详情编辑 | Edge UI/IPC 模拟 + 组件单测 | 通过 |
| V2-06 | 内联保存失败保留输入并显示中文错误 | Edge UI/IPC 故障注入 + 组件单测 | 通过 |
| V2-07 | 一键备份无需文件对话框并反馈本地路径 | Edge UI/IPC 模拟 + Rust 测试 | 通过 |
| V2-08 | 导出面板可三维多选，TXT 导出反馈成功 | Edge UI/IPC 模拟 + Rust 格式测试 | 通过 |
| V2-09 | TXT 分组、排序、去重、过滤、日期边界、UTF-8 格式 | Rust `cargo test` | 通过 |

## 执行结果

- `npm run build`：通过。
- `npm test`：通过，5 个文件、26 项测试；其中新增 5 项 ItemList 保存保护测试覆盖延迟保存等待、等待期间追加保存、失败阻止离开并保留输入、进度失败不被普通成功掩盖、详情编辑期间卡片只读。
- `cargo test`：通过，7 项测试；覆盖 SQLite 持久化/历史、恢复安全备份、快速备份不覆盖、TXT 导出筛选/分组/排序/格式与本机日历边界。
- `QA_UI_READY=1 npm run test:e2e`：通过，Edge 5 项 UI/IPC 模拟流程。测试使用隔离端口 4173 与本机 Edge，不下载浏览器。

## 覆盖边界与未覆盖项

Edge 流程通过页面注入的 Tauri IPC、对话框和窗口对象双替身运行；其结论仅说明前端可按契约调用接口和呈现反馈，**不能**替代真实 SQLite、真实文件系统、Windows 文件对话框或 Tauri 桌面运行时验证。

`cargo test` 使用隔离临时数据覆盖后端数据与导出规则，但没有启动桌面窗口。最终复测没有发现需退回业务开发员的缺陷。尚待 PM 在隔离的 `WORK_MEMO_DATA_DIR` 中人工核验：真实一键备份与 TXT 文件实际内容/编码、文件选择与确认对话框、关闭后重开、窗口尺寸与键盘操作。以上为未覆盖项，不是本轮失败。
