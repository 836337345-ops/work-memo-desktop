# V2.12 验收修复独立 QA 报告

- 验收日期：2026年9月12日
- 验收任务：T126 独立 QA
- 固定验收提交：`677b0a6e7c878f8d0bfe44beaf6dfc27cc7abd95`
- 业务固定提交：`7a08742ec01433d1dc42eb2ce533a8b6df091a57`
- 验收分支：`codex/v212-acceptance-qa`
- 结论：**通过，未发现可稳定复现的业务缺陷。**

## 验收范围与结果

1. 启动默认进入工作日历；日历的进行中、已完成、已暂停状态可多选，并与类别条件叠加。事项卡、编辑器和模板路径均先通过左栏“全部”离开日历后复核。
2. 跟进清单改动在收起、焦点离开、切换事项、删除或离开页面前自动保存；失败会保留草稿并阻断原动作。已完成项置于未完成项之后，显示红色加粗“已完成”。
3. 事项卡可切换“重要事项”，标题与日历显示星标；新建事项可勾选重要事项。删除需确认后移入回收站；回收站还原后回到原列表，不会打开新建编辑栏。
4. 新建和修改保存时会清理空白跟进项；“＋ 添加清单”与清单模板设置、创建、编辑、删除和应用均可用。
5. SQLite 旧数据库迁移默认未星标且可重复执行；新备份保留星标，缺少字段的旧备份默认未星标，恢复失败不改变原数据。

## 验证结果

| 检查 | 结果 |
| --- | --- |
| 全量前端测试 | 14 个文件、110 项通过 |
| Rust 与 SQLite 测试 | 15 项通过，0 项失败 |
| 前端构建 | `npm run build` 通过 |
| 差异格式检查 | `git diff --check` 通过 |
| 隔离 Edge 关键路径 | 1 项通过：默认日历、状态筛选、离开日历、重要事项、跟进完成及收起自动保存 |

## 数据与测试边界

- 全部浏览器和数据层验证使用隔离的虚构数据；未读取正式用户数据、未运行安装包、未触碰 Windows 启动项。
- 旧的 V2.8 Playwright“启动默认进行中列表”断言已按当前契约排除，不作为失败依据。
- 真实 Windows 窗口、缩放与用户已有数据升级效果仍应由用户在固定提交调试版中验收。
