# V2.5 独立验收报告

## 被测版本与边界

被测集成提交：`09aeb64`（`feat: enhance work calendar details`）。验收分支为 `qa/v2.5`；本轮仅修改 `tests/**` 与本报告，未修改业务代码、未连接远程仓库、未生成安装包。

全部界面测试通过独立 Edge 实例、独立 4285 端口和内存 IPC 替身运行。测试中的事项、分类、模板、备份路径均为虚构数据，未读取、写入或覆盖用户数据。产物报告置于 `D:\qa-v25-playwright-report`，构建输出置于 `D:\qa-v25-dist`。

## 逐项验收

| 编号 | 验收点 | 结果 | 验证与复现步骤 |
| --- | --- | --- | --- |
| V25-01 | 同一日 2 项显示两行；最多直显 3 行；超过显示“另有 N 项”；悬停及键盘聚焦可见全部标题 | 通过 | 注入同日 5 条虚构事项；断言直显前三条及“另有 2 项”，再分别悬停、聚焦日期，提示层均列出第五条和未分类事项。 |
| V25-02 | 日历分类默认全部；真实分类和未分类可多选；清空恢复全部；仅影响日历，重新进入恢复全部，不改变左侧筛选 | 通过 | 先在左侧选“包装”，进入日历后选“活动”与“未分类”，确认日历内容变化；点“全部”、关闭再进入，确认左侧仍为“包装”，日历筛选恢复全部。 |
| V25-03 | 日期字号较旧版增大；周六日及节日日期红色 | 通过 | Edge 计算样式确认日期为 14px（旧版 `9791189` 为 13px）；周日与中秋日期均带红色状态类，周日实际颜色为 `rgb(199, 84, 77)`。 |
| V25-04 | 日期首行同行展示 8 类节日；覆盖跨年六周格；2020-10-01 合并显示中秋/国庆；不把调休当功能 | 通过 | 节日契约测试覆盖元旦、春节、清明、劳动节、母亲节、端午、中秋、国庆以及 `2020-10-01=中秋/国庆`；Edge 确认元旦与日期同行，2026 年 12 月六周格显示 2027-01-01 元旦。未测试调休，符合范围。 |
| V25-05 | 日历固定 42 格、周一开头、只读；排除回收站和无日期；忽略工作台筛选；四色状态正确 | 通过 | 单元及 Edge 回归断言 42 格、周一开头、四种标记色、工作台筛选不影响日历；虚构无日期和回收站事项均不出现，网格内无编辑输入控件。 |
| V25-06 | 最新进度标题与正文同行起始；长内容和换行保留；点击更新及提交后行为不退化 | 通过 | 使用含换行的长虚构进度，断言 CSS 网格同行、`pre-wrap`、无横向溢出；点击正文提交新进度后输入框关闭，正文更新为新内容。 |
| V25-07 | 1060×700 与小高度下底部可访问、没有双滚动 | 通过 | Edge 回归在 1060×700 和 1060×360 下滚到底部；确认仅日历内部可滚动，列表容器和文档不产生第二纵向滚动。 |
| V25-08 | 回归新增/编辑、进度、跟进模板、备份恢复、筛选及 TXT 导出 | 通过 | 完整 Edge 套件覆盖新建、编辑失败保留、进度成功/失败、模板应用/新增删除、备份、校验并恢复、TXT 导出、组合筛选、回收站及返回流程。 |

## 执行结果

| 命令 | 结果 |
| --- | --- |
| `npm test` | 通过：11 个测试文件，55 项测试。 |
| `QA_UI_READY=1 npx playwright test --config tests/playwright.v25.config.ts` | 通过：Edge 18 项流程全部通过。 |
| `npm run build -- --outDir D:\qa-v25-dist` | 通过：TypeScript 检查及前端构建完成。 |
| `git diff --check` | 通过：无空白错误。 |

## 失败、未验证与遗留问题

本轮无失败项，也未发现需退回修复的业务缺陷。

以下项目为**未验证**，不能据此视为通过：真实 Tauri 桌面窗口的系统字体观感、真实 SQLite 重启后的持久化、系统文件对话框的实际读写与真实备份恢复。它们需要在隔离应用数据目录中运行调试版进行人工桌面验收；本轮按约束没有接触真实用户数据，也没有生成安装包。
