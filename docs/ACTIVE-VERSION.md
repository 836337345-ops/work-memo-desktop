# 当前开发状态

> 这是项目唯一的当前版本来源。已完成版本的详细任务、测试和用户验收记录留在各自任务卡、QA 报告和版本交接文件中，避免在此重复累积。

最后更新：2026年9月12日

## 当前状态

- 当前版本：V2.12，阶段为“用户确认”。产品内部版本号仍为 `0.1.1`，本轮暂不打包。
- 用户已明确确认 V2.11 通过并授权打包，随后确认版本号使用 `0.1.1`；允许生成安装包并在发布 QA 通过后合入 `main`。
- 发布任务卡为 `docs/TASK-V2.11-RELEASE.md`；发布基线为 `e9b8c1baeb8f6ccd8116998d15e09b363a8b474d`。T114 已启动，正式任务 ID 为 `01a08777-ecb2-7c73-8e4a-d09fcd47f4e7`，工作树为 `C:/Users/Administrator/.codex/worktrees/4a8b/工作备忘录`，分支为 `codex/v211-release-011`，起始 HEAD 为 `aa4a045969967f5fd4e977498c254dc6310a78ef`。
- V2.10 已获用户确认通过，属于历史记录；当前只处理 V2.11 发布。
- V2.11 固定基线为 `13b3b87b99df7d7e49aed6a7405edcdc7244aa2d`，任务卡为 `docs/TASK-V2.11.md`，分级为涉及 Windows 启动项的普通功能。
- 唯一固定发布提交：`0e827de285d176db7dc2b6dfdea88cf232b658a3`；候选包已生成，`main` 尚未修改，未上传或发布。
- 独立发布 QA 已在固定提交 `0e827de` 上通过；版本声明、候选包、哈希、文件版本、签名和安装边界均符合要求。
- V2.12 固定基线为 `e01e361e77995364a6f5c3e276d798d1d0ef0f51`，任务卡为 `docs/TASK-V2.12.md`，六项需求已由用户确认；因星标持久化涉及 SQLite 兼容，分级为数据安全功能。
- 唯一下一步：创建一项可见 Terra Medium 开发任务，完成启动验真后进入开发；不得再向 V2.11 发布提交追加功能。

## T101 开发任务

> 历史：V2.10 已通过用户验收，本节不作为当前待办。

- 任务卡：`docs/TASK-V2.10.md`。
- 窗口名称：`V2.10 开发｜事项卡、日历与跟进修复`。
- 正式任务 ID：`01a08725-6026-7d63-b3fb-317a78b5e615`。
- 工作树：`C:/Users/Administrator/.codex/worktrees/c326/工作备忘录`。
- 分支：`codex/v210-card-calendar-followup`。
- 当前 HEAD：`5e84011e7c12af5690a305c8ea5a3c11d2f32d05`。
- 当前状态：用户验收界面补丁已完成并集成。
- 已完成：开发员完成任务卡全部范围；首次提交后按 PM 审查意见修正了图标结构、半角分隔符、空白跟进过滤及错误提示清理边界；工作树保持干净。
- 开发员自测：相关测试通过；完整前端测试 `94/94` 通过；`npm run build` 通过；差异格式检查通过。为避免影响已运行程序和正式数据，未执行桌面短时界面检查。
- 集成结果：开发提交已一次集成到 `integration/v2`，对应集成提交为 `8d31011`、`d43eabe`；唯一固定 QA 提交为 `d43eabee5fa4cfc7bb517ef6d9d01c12568dcc7c`。
- PM 集成检查：受影响的 5 个测试文件共 `55/55` 项通过；`npm run build` 通过；未重复执行完整测试。
- 独立测试结果：固定提交 `d43eabe` 的 14 个测试文件共 `94/94` 项通过，`npm run build` 通过；七项需求的自动化与样式契约核验通过，未发现可稳定复现的业务缺陷。
- 环境限制：隔离桌面调试实例因本机 `127.0.0.1:1460` 端口权限错误（`EACCES`）未能启动；未连接现有实例、未触碰正式数据，因此真实点击与视觉目测仍需用户调试验收补充。
- 用户验收反馈：左侧筛选组展开/收起按钮需移到标题最左侧；事项卡首行在窄宽度下被拆成多行，需强制保持“日期 | 类别 | 标题”单行，并在空间不足时仅省略过长标题。
- 修复分级：纯界面微调；退回原 T101 开发员，执行相关测试和构建，PM 审查集成后由用户复验；不创建重复开发任务或重复完整测试。
- 补丁交付：`5e84011`；修改范围仅为 `Sidebar`、`ItemList` 组件、样式及对应测试，工作区干净。
- 补丁自测：相关组件测试 `26/26` 通过，`npm run build` 通过，差异格式检查通过；按微调分级未重复完整测试。
- 补丁集成：开发提交 `5e84011` 已集成为 `564d35f74e3f229716ca103be15c0840d200b172`；未混入其他业务功能。
- 调试验证：基于 `564d35f` 的无安装包调试程序已成功构建并启动，进程 `6704` 响应正常；继续使用隔离数据目录 `C:/Users/Administrator/AppData/Local/Temp/work-memo-v210-user-5eca45e`。
- 正在做：等待用户复验左栏图标位置和事项卡首行单行显示。
- 下一步：用户确认通过后，阶段才可进入“等待打包授权”；本补丁不另开独立 QA。
- 未完成：用户调试验收及后续单独的打包授权。
- 未提交文件：无。
- 交付提交：`c848d79`、`deaab2f`、`5e84011`。

## T102 独立测试任务

> 历史：V2.10 独立测试已通过，本节不作为当前待办。

- 窗口名称：`V2.10 独立测试｜固定集成验收`。
- 正式任务 ID：`01a08747-1520-7792-9dd7-5ae525e0c7ca`。
- 工作树：`C:/Users/Administrator/.codex/worktrees/81cc/工作备忘录`。
- 分支：`codex/v210-independent-qa`。
- 固定提交：`d43eabee5fa4cfc7bb517ef6d9d01c12568dcc7c`。
- 环境状态：依赖脚本通过；`node_modules` 与 Rust `target` 均已链接到共享目录。
- 当前状态：独立测试完成并通过；验收报告为 `docs/QA-V2.10.md`，报告提交为 `b9ea098`、`976459a`，已纳入 `integration/v2`（`f563d95`、`aae9d77`）。
- 允许修改：仅测试文件与 `docs/QA-V2.10.md`；禁止修改业务代码。

## 下一步与边界

- V2.11 可见开发任务已通过启动验真；不得创建重复任务或由 PM 修改业务代码。
- V2.11 必须使用 Tauri 官方自启动插件和最小权限，不得用注册表脚本或计划任务替代；开发与测试不得改变正式安装版启动项。
- T114 已完成版本更新、候选包构建和一次集成；发布 QA 只核验候选包和版本边界，不重复完整业务测试。
- 发布 QA 通过前不得合入 `main`；全程不得上传、远程推送、删除旧安装包或清理工作树。

## T114 发布任务

- 任务卡：`docs/TASK-V2.11-RELEASE.md`；窗口名称：`V2.11 发布｜Windows 0.1.1 安装包`；正式任务 ID：`01a08777-ecb2-7c73-8e4a-d09fcd47f4e7`。
- 工作树：`C:/Users/Administrator/.codex/worktrees/4a8b/工作备忘录`；分支：`codex/v211-release-011`；起始提交：`aa4a045969967f5fd4e977498c254dc6310a78ef`。
- 交付提交：`296ecab`；PM 已审查并一次集成为固定发布提交 `0e827de285d176db7dc2b6dfdea88cf232b658a3`。
- 已完成：项目版本一致更新为 `0.1.1`；正式 Windows x64 NSIS 候选包已生成，大小 `2,859,446` 字节，SHA-256 为 `A233AA0108024B6B853260C55BAA40C717CD963DD2FBDEA1638E3E6DF4D1E95D`，文件及产品版本均为 `0.1.1`，签名状态为未签名。
- 发布员验证：`npm run build`、Rust 测试 `14/14`、正式安装包构建和差异格式检查均通过。
- PM 审查：仅包含任务卡允许的 6 个版本与发布报告文件；版本声明、候选包哈希、Windows 版本和未签名状态已复核；旧版 `0.1.0` 安装包仍在原归档位置。
- 已完成：T115 在同一测试窗口完成口径复核并通过，报告提交为 `d9ac437`，已集成为 `7c8e119`；未重新构建或重复业务测试。
- 下一步：由 PM 按既有授权将 `integration/v2` 快进合入 `main`；不上传、不推送。随后建立已确认的 V2.12 任务卡和可见开发任务。
- 未提交文件：无。

## T111 开发任务

- 任务卡：`docs/TASK-V2.11.md`。
- 窗口名称：`V2.11 开发｜开机自启动开关`。
- 正式任务 ID：`01a08763-a2eb-7d62-b6b6-3f7e1ab1dd10`。
- 工作树：`C:/Users/Administrator/.codex/worktrees/4e11/工作备忘录`。
- 分支：`codex/v211-autostart-toggle`。
- 起始提交：`721bb7d455a10e97bbe9cbf0a9f5570537f0ac0d`；业务基线为 `13b3b87b99df7d7e49aed6a7405edcdc7244aa2d`。
- 环境状态：依赖脚本通过；`node_modules` 与 Rust `target` 均已链接共享目录；工作区干净。
- 当前状态：开发完成并一次集成，等待固定提交独立 QA。
- 交付提交：`5ea26b76db8a8eb36c205fe670c4fa96d963b6f3`；工作区干净。
- 已实现：官方 Tauri 自启动插件、三项最小权限、左栏底部可访问开关、真实状态初读和切换后复核、失败回滚与连续点击保护。
- 开发员自测：定向测试 `27/27`、完整前端测试 `101/101`、Rust 测试 `14/14`、`npm run build` 和差异格式检查全部通过。
- 安全边界：未触碰正式安装版或真实 Windows 启动项；本机缺少 `rustfmt` 仅导致依赖添加命令提示，未影响构建和测试，未擅自安装环境组件。
- 集成结果：开发提交 `5ea26b7` 已一次集成为 `900e80d`；唯一固定 QA 提交为 `900e80d`。
- PM 集成检查：自启动相关前端测试 `27/27` 通过，Rust `cargo check` 通过；未重复完整测试。首次前端检查因共享依赖尚未同步而无法加载官方插件，执行 `npm install` 后恢复，未产生源码或锁文件变化，确认为环境问题。
- 下一步：创建 Terra Medium 独立测试任务，只在固定提交 `900e80d` 上验收；不得混入其他功能。
- 未提交文件：无。

## T112 独立测试任务

- 窗口名称：`V2.11 独立测试｜开机自启动`。
- 正式任务 ID：`01a0876c-bd29-7df0-b149-306711e81103`。
- 工作树：`C:/Users/Administrator/.codex/worktrees/3898/工作备忘录`。
- 分支：`codex/v211-independent-qa`。
- 固定提交：`900e80dff7b1ac658defd8d1339fbefbf2d36df5`。
- 环境状态：依赖脚本通过，前端与 Rust 共享依赖可用，工作区干净；测试员已发出第一条实际进度。
- 当前状态：独立测试完成并通过；前端测试 `101/101`、Rust 测试 `14/14`、构建与差异格式检查全部通过，未发现业务缺陷。
- 验收报告：`docs/QA-V2.11.md`，测试分支提交 `40b07d5`，已纳入 `integration/v2`（`b418799`）。
- 安全限制：未写入真实 Windows 启动项，实际登录后启动只能在正式安装版阶段验证。
- 用户调试：已基于固定业务提交 `900e80d` 生成无安装包调试程序并短时验证启动；进程 `19588`、窗口“工作台”响应正常，数据目录隔离为 `C:/Users/Administrator/AppData/Local/Temp/work-memo-v211-user-900e80d`。
- 允许修改：仅测试文件与 `docs/QA-V2.11.md`，不得修改业务代码。

- T101 因用户验收缺陷重新打开；继续使用原任务、原工作树与原分支，不新建重复开发任务、不重复执行完整测试。
- 未经明确授权，不得重新打包、修改 `main`、上传、发布、清理工作树或读取正式用户数据。
- T101 工作树 `C:/Users/Administrator/.codex/worktrees/c326/工作备忘录` 必须保留，直至开发、集成、QA 和用户验收完成并取得清理授权。
- 旧工作树 `C:/Users/Administrator/.codex/worktrees/f042/工作备忘录` 有未提交修改，必须继续保留并另行确认后处理。
- 独立测试已在固定提交 `d43eabe` 完成；后续不得把新增功能混入本轮用户验收版本。

## 事实来源

- 当前产品规则：`docs/PROJECT-HANDOFF.md`。
- V2.10 最终需求与任务边界：`docs/TASK-V2.10.md`。
- V2.9 开发、测试和交付证据：`docs/HANDOFF-V2.9.md` 与 `docs/QA-V2.9.md`。
- PM 用量审计：`docs/HANDOFF-SOL-USAGE-20260909.json`。
