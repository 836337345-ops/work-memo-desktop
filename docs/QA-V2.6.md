# V2.6 日历操作增强独立验收报告

## 被测版本与边界

被测集成提交：`7c5fe92`（`feat: connect calendar actions to workbench`，来自 `integration/v2`）。验收分支：`qa/v2.6`。本轮仅修改 `tests/**` 与本报告；未修改业务代码、未连接远程仓库、未生成安装包、未合入 `main`。

验收在独立 Edge 实例、独立 `4286` 端口和内存 IPC 替身中完成。事项、分类、模板和备份路径均为虚构数据；未读取、写入或覆盖任何用户数据。浏览器报告位于 `D:\qa-v26-playwright-report`。

说明：委托中列出的 `docs/CONTRACT-V2.6.md` 和 `docs/TASKS-V2.6.md` 在被测提交中不存在；V2.6 的实际冻结要求位于 `docs/CONTRACT.md` 的“V2.6 日历操作增强”部分。本报告按该已提交公共契约执行。

## 逐项验收

| 编号 | 验收点 | 结果 | 验证方式 |
| --- | --- | --- | --- |
| V26-01 | 每条直显事项和浮窗事项均显示自身状态的呼吸灯；待开展灰、进行中绿、已暂停红、已完成橙 | 通过 | 向同一日期注入四种状态的虚构事项，检查四种逐事项状态类、计算颜色与呼吸动画。 |
| V26-02 | 日期格不再有旧版按状态汇总数量/标记；仍最多直显 3 条，并以“另有 N 项”提示超量 | 通过 | 同日 4 项时仅显示前三项标题和“另有 1 项”；旧汇总状态元素数量为 0。 |
| V26-03 | 所有日期均可打开浮窗；空日期显示“当日暂无事项”并提供“新增计划” | 通过 | 聚焦无事项的 2026-09-10，确认空提示及可点击的新增入口。 |
| V26-04 | 从浮窗点击事项会退出日历、清除搜索及状态/时间/分类遮挡条件、返回工作台并定位高亮展开；不打开右侧编辑栏 | 通过 | 先设置四类会遮挡目标的条件，再点浮窗目标事项；确认“全部事项”、搜索框清空、三类筛选恢复全部、卡片高亮且展开、编辑栏未打开。 |
| V26-05 | 重复点击同一浮窗事项仍定位高亮展开且不打开编辑栏 | 通过 | 再次进入日历并点击同一事项，重复确认高亮、展开及无编辑栏。 |
| V26-06 | 有事项日期、空日期和跨月日期均能从“新增计划”进入新建栏，准确预填日期并保存 | 通过 | 分别选择 2026-09-07、2026-09-10、2026-10-15；逐次检查新建截止日期和三次虚构 `create_item` 请求的日期、标题。 |
| V26-07 | 日历分类、节假日、滚动、事项卡、筛选及原有工作台流程不退化 | 通过 | 完整 Edge 回归覆盖 19 项：组合筛选、回收站、编辑、进度、模板、备份恢复、导出、六周日历、节假日、分类多选、窄高滚动等。旧日历颜色回归断言已更新为逐事项状态灯，符合 V2.6 取代旧汇总标记的契约。 |

## 执行结果

| 检查 | 结果 |
| --- | --- |
| `npm test` | 通过：11 个测试文件、62 项测试。 |
| `QA_UI_READY=1 npx playwright test --config tests/playwright.v26.config.ts` | 通过：Edge 19 项流程全部通过。 |
| `npm run build` | 通过：TypeScript 检查及前端构建完成。 |
| `git diff --check` | 通过：无空白错误。 |

## 失败、未验证与遗留问题

未发现需退回修复的业务缺陷。

以下项目未在本轮自动验收中覆盖，不能据此视为通过：真实 Tauri 桌面窗口的系统字体和动画观感、真实 SQLite 重启后的持久化、系统文件对话框及真实备份恢复。它们应在隔离应用数据目录中运行调试版后进行人工桌面验收；本轮依约未生成安装包，也未接触真实用户数据。

文档遗留：如 PM 需要按独立文件追踪 V2.6，应补充委托中引用的 `docs/CONTRACT-V2.6.md` 和 `docs/TASKS-V2.6.md`，或更新委托统一指向 `docs/CONTRACT.md` 的 V2.6 小节；这不影响本次已按现有冻结要求完成的功能验收。
