# V2.3 独立验收报告

## 被测版本与范围

固定被测集成提交：`cc38075`（`feat: add show-all filter reset and date formatting`）。测试数据全部为虚构的地产推广事项；Edge 流程使用内存 IPC 双替身，不读取真实用户数据。本轮只修改 `tests/**` 与本报告。

| 编号 | 验收点 | 验证方式 | 结果 |
| --- | --- | --- | --- |
| V23-01 | 点击“最新进度”标题或正文才显示输入；提交成功只隐藏输入并保持卡片展开 | Edge UI/IPC 模拟 + ItemList 单测 | 通过 |
| V23-02 | 进度失败保留输入、错误可见并保持展开 | Edge 故障注入 + ItemList 单测 | 通过 |
| V23-03 | 详情栏只保留标题、类别、截止日期、事项情况、备注、跟进清单及模板；状态和最新进度不在右栏 | Edge UI/IPC 模拟 + ItemEditor 单测 | 通过 |
| V23-04 | 详情栏只有 `.item-editor__scroll` 滚动，外层 `.editor-pane` 不滚动；顶部保存区固定，底部删除入口可访问 | Edge DOM/UI 验收（1060×360） | 通过 |
| V23-05 | “显示全部”清除状态、时间、分类、搜索并退出回收站 | Edge UI/IPC 模拟 + Sidebar 单测 | 通过 |
| V23-06 | 事项日期和备份时间统一显示中文年月日/年月日时分格式 | Edge UI/IPC 模拟 + dateFormat/BackupPanel 单测 | 通过 |
| V23-07 | V2.2 卡片、编辑器、模板 CRUD、筛选/历史/逾期、备份与 TXT 导出回归 | Edge UI/IPC 模拟 | 通过 |

## 执行结果

- `npm run build -- --outDir D:\qa-v23-dist`：通过（等价构建验证；输出目录因当时宿主 C 盘无可用空间定向到隔离 D 盘）。
- `npm test`：通过，7 个文件、39 项测试。
- `cargo test`：通过，11 项测试；使用隔离 `D:\qa-v23-cargo-target`，无业务代码改动。
- `QA_UI_READY=1` Edge E2E：12 项流程全部通过。为规避当时宿主 C 盘 0 字节可用导致 WebServer 在单次长跑中途拒绝连接，分为 6 + 3 + 3 三组执行；失败的是环境启动，不是断言或产品行为。输出均写入隔离 D 盘目录。

## 未覆盖项与 PM 人工验收

本轮未发现需退回业务开发员的缺陷。仍需 PM 在隔离 `WORK_MEMO_DATA_DIR` 中人工核验真实 SQLite 持久化、Windows 文件对话框、真实桌面窗口缩放/重开、系统字体与键盘无障碍。以上是自动化边界，不是本轮失败。

## PM 集成补充

- QA 用例已合入 `integration/v2`；PM 在主集成目录复跑 `npm test -- --run`，39 项全部通过，`npm run build` 通过。
- PM 在主集成目录补跑 `cargo test --manifest-path src-tauri/Cargo.toml`，11 项 Rust 数据、备份、恢复、导出和模板测试全部通过。
- Windows 调试进程 `work-memo.exe` 正常响应，开发服务 `http://127.0.0.1:1460` 返回 200；当前窗口标题为“工作备忘录 · V2 调试版”。
- 本次会话的 Windows 自动操作连接器未暴露原生应用窗口，因此没有把真实 Tauri 窗口点击和截图检查记为通过。小窗口、单滚动区、固定顶部按钮和底部可达已由 Edge 1060×360 隔离 UI 验收覆盖，仍保留用户在真实调试窗口中的最终体验确认。
- 按用户约定，本版本不生成安装包、不合入 `main`，等待用户明确说“确认通过，可以打包”。

### 2026-09-07 中断恢复复核

- C 盘空间恢复后继续原集成点，未重复修改业务功能；构建与 Rust 编译缓存继续定向至 D 盘。
- `npm test -- --run` 39 项、`cargo test` 11 项、Edge E2E 12 项均在本次恢复后再次完整通过，前端构建通过。
- 调试版已使用独立目录 `artifacts/v2-3-user-test-data` 重新启动；SQLite 文件已正常创建，调试进程与 1460 端口正常响应。
