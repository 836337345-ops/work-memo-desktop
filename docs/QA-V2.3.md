# V2.3 独立验收报告

## 被测版本与数据隔离

- 固定被测集成提交：`cc38075`（`feat: add show-all filter reset and date formatting`）。
- QA 分支：`qa/v2.3`。
- 浏览器验收使用 Edge 与内存 IPC 双替身；全部为虚构的推广、活动和包装事项，模拟路径为 `C:/qa-isolated`。未读取、写入或恢复用户正式数据，也未启动正式 Tauri 桌面程序。

## 验收结果

| 编号 | 验收点 | 验证方式 | 结果 |
| --- | --- | --- | --- |
| V23-01 | 详情编辑栏仅有标题、类别、截止日期、事项情况、备注、跟进清单和模板；不含状态或进度 | Edge UI/IPC 模拟 + DOM 语义断言 | 通过 |
| V23-02 | 编辑栏只有内部一个纵向滚动区；顶部保存区在滚到底部后仍固定且可用 | Edge 小窗口（1060×360）布局断言 | 通过 |
| V23-03 | 点击“最新进度”标题才显示输入框；成功只关闭输入框并保留展开状态 | Edge UI/IPC 模拟 | 通过 |
| V23-04 | 进度提交失败时保留输入、错误提示及卡片展开状态 | Edge 故障注入 | 通过 |
| V23-05 | “显示全部”清除搜索、状态、时间、分类，并退出回收站显示正常事项 | Edge UI/IPC 模拟 | 通过 |
| V23-06 | 事项日期为 `X年X月X日`，备份时间为 `X年X月X日 HH:mm` | Edge UI/IPC 模拟 + 前端单测 | 通过 |
| V23-07 | 跟进模板的类别隔离、应用去重、创建/删除和失败提示无回退 | Edge UI/IPC 模拟 + 前端单测 | 通过 |
| V23-08 | 状态、组合筛选、备份、TXT 导出及小窗口底部内容访问无回退 | Edge UI/IPC 模拟 | 通过 |

## 自动化执行

- `npm test`：通过，7 个测试文件、39 项测试。
- `npm run build`：通过。
- `QA_UI_READY=1 npx playwright test --reporter=list`：通过，Edge 12 项隔离 UI/IPC 模拟验收。
- `cargo test`：已尝试；首次完整编译在生成 Rust 测试归档时因 C 盘空间不足（`os error 112`）停止，未产生可用的 Rust 测试结论。编译缓存已移至 D 盘的临时 QA 缓存位置以释放工作空间；未修改 Rust 业务代码或用户数据。

## 未测项与结论

未执行真实 SQLite、正式 `WORK_MEMO_DATA_DIR`、Windows 文件对话框、跨进程重启持久化及正式 Tauri 窗口缩放人工检查；浏览器双替身不能替代这些桌面运行时验证。由于 Rust 测试受机器磁盘空间阻断，本报告不将其计为通过。

在前端构建、39 项单元/契约测试和 12 项隔离浏览器验收范围内，V2.3 契约全部通过，未发现需退回业务开发的缺陷。建议 PM 在具备充足磁盘空间的隔离环境补跑 `cargo test`，并进行上述真实桌面人工验收后再决定发布。
