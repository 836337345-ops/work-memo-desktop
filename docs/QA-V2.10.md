# V2.10 独立验收报告

验收日期：2026 年 9 月 10 日
验收人：独立测试员
被测提交：`d43eabee5fa4cfc7bb517ef6d9d01c12568dcc7c`（`fix: harden item follow-up saves`）
验收分支：`codex/v210-independent-qa`

## 数据边界

- 已在独立工作树和指定固定提交上验收，未读取正式用户数据。
- 自动化测试使用组件模拟数据；桌面短时检查预设了隔离目录 `WORK_MEMO_DATA_DIR=C:\Users\Administrator\AppData\Local\Temp\work-memo-v210-qa-d43eabee`，其中仅允许虚构数据。

## 自动化结果

- `npm test -- --run`：通过，14 个测试文件、94 项测试通过、0 项失败。
- `npm run build`：通过；类型检查及生产构建均完成。
- 固定提交差异格式检查：通过，未发现空白错误。

## 七项需求核验

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 日期正序、无日期最后、同日新建优先 | 通过 | `src/lib/filters.test.ts` 覆盖空日期置后及同日按创建时间倒序。 |
| 事项卡两行、精确半角 ` | ` | 通过 | `ItemList` 测试断言首行文本为“日期 | 类别 | 标题”，第二行保留完整最新进度。 |
| 双横线加箭头及方向 | 通过 | 侧栏与事项卡均使用两条横线和独立箭头路径；测试验证展开为向上、收起为向下，保留 `aria-expanded`。 |
| 默认进入工作日历 | 通过 | `App` 测试验证启动即显示当前月日历，快捷入口能回到事项列表。 |
| 左栏深绿、短文案与顺序 | 通过 | 测试验证“工作日历 → 进行中 → 全部”的顺序和入口行为；样式复核确认日历按钮使用深绿色底色。 |
| 跟进保存、失败重试、空白过滤与错误清理 | 通过 | `ItemList` 测试覆盖空白项不提交、失败时保留输入、后续成功时清除卡片和页面旧错误，以及进度错误不被普通保存误清。 |
| 日历表头与日期右对齐 | 通过 | `WorkCalendar` 测试确认表头右对齐、日期行右侧对齐及一致内边距。 |

## 短时桌面检查

未能执行，原因是隔离调试启动在创建窗口前失败：Vite 无法绑定本机 `127.0.0.1:1460`，报错为 `EACCES: permission denied`。未连接任何现有实例，未影响正式数据；该问题属于当前环境端口权限限制，不能据此判定业务功能失败。

## 结论

自动化独立验收通过，未发现可稳定复现的 V2.10 业务缺陷。桌面实际点击与视觉检查因环境端口权限限制未覆盖，交由后续用户调试验收补充确认；在该限制解除前，不应将其写作“桌面界面检查已通过”。
