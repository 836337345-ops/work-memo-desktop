# 公共契约与任务卡

## V2.8 状态、跟进与导出调整（2026-09-08）

开发前快照固定为本地标签 `v2.7-before-workflow-v28-20260908`，指向提交 `272ad96`。本轮只交付开发/调试版本；用户明确说“确认通过，可以打包”前禁止生成安装包或合入 `main`。

事项状态取消“待开展”，只保留进行中、已完成、已暂停；新建事项默认进行中。用户已确认当前环境中的“待开展”事项均为测试数据，可以直接清除，不迁移为其他状态。V2.8 首次打开既有数据库及恢复旧备份时清除状态为 `todo` 的事项及其关联记录，不影响其他状态事项；新的保存、查询与导出接口不再接受 `todo`。逾期只指截止日期早于本机今天且状态为进行中的事项。

新建数据库的默认分类统一为推广、销售、拓展、包装、礼品、事务、活动、其他；既有数据库和恢复备份中的自定义分类原样保留，不用默认分类覆盖用户已有分类。

程序启动默认显示进行中事项，时间与分类不限。左栏“显示全部”下新增“显示进行中工作”；点击后退出日历/回收站、清除搜索，只保留进行中筛选，时间和分类恢复全部。状态、时间、分类收起时统一紧凑显示为“标题 · 当前条件”，不得用弹性空白把两段文字分开。

事项编辑栏顶部固定显示“保存并关闭”和“删除事项”；新建时不显示删除。删除仍执行可恢复的移入回收站，按钮文案改为“删除事项”，沿用现有确认规则；底部不再重复显示删除入口。跟进清单编辑区第一行左侧为蓝色“添加清单”，右侧为蓝色“模板设定/应用”；新建、改名、删除和应用模板的入口统一收纳在“模板设定/应用”内，具体跟进项显示在该行下方。事项卡展开后的“添加跟进”紧跟“跟进清单”标题同行显示。

导出面板的状态、时间、分类三组各提供“全选”和“反选”。时间可见选项与左栏匹配为今天、本周、下周、本月、下月、历史，不显示“全部时间”和“已逾期”。时间全选代表真正不限制日期，包含未设日期和下月之后日期；任一组反选后若没有选项，禁止导出并提示至少选择一项。状态仅提供进行中、已完成、已暂停；分类包含全部现有分类及未分类。导出格式、分类/日期排序与 TXT 内容规则保持不变。

事项卡副标题中的类别与日期字号提高为 14px，仍低于 16px 的事项标题；中文长类别与日期不得挤压标题行操作。

### V2.8 并行边界

T91 A（`feat/status-export-contract-v28`）：负责公共状态类型、默认值、Rust 校验与 SQLite/备份兼容清理、逾期规则及数据测试；可修改 `src/types.ts`、`src/lib/filters.ts`、`src-tauri/src/lib.rs` 和直接相关测试，不改界面布局。

T92 B（`feat/editor-followups-v28`）：负责编辑栏顶部删除、跟进清单管理布局、事项卡添加跟进入口与副标题字号；只修改 ItemEditor、ItemList、对应 CSS 与直接相关测试，不改公共类型、筛选、导出或 Rust。

T93 C（`feat/sidebar-export-v28`）：负责启动默认筛选、显示进行中工作、左栏紧凑摘要、导出全选/反选及选项；可修改 App、Sidebar、ExportPanel、对应 CSS 与直接相关测试，不改 Rust 或编辑器。

T94 QA（`qa/v2.8`）：固定 PM 指定的集成提交，独立验收状态清理、默认筛选、编辑与跟进布局、导出选择语义及全量回归；只修改测试与 `docs/QA-V2.8.md`，不代改业务代码。

## V2.7 shadcn 风格视觉优化（2026-09-07）

开发前快照固定为本地标签 `v2.6-before-shadcn-visual-20260907`，指向提交 `89d84c1`。本轮只交付开发/调试版本；用户明确说“确认通过，可以打包”前禁止生成安装包或合入 `main`。

本轮保留现有浅绿色品牌主色，将全界面统一为简洁、克制的 shadcn 风格：使用语义化 CSS 设计令牌统一背景、前景、卡片、弹层、主次按钮、弱化区域、边框、输入框、焦点环、侧栏、圆角、阴影、字号和间距。优化范围覆盖左侧导航、工作台标题与搜索、事项卡、右侧编辑栏、工作日历、分类管理、备份恢复、导出及各类空状态/错误提示。

第一阶段不引入 Tailwind、Radix、`components.json` 或 shadcn CLI，不安装新的 UI 依赖；保留现有 React 组件、DOM 主结构、可访问名称与业务回调。不得改变事项增删改、自动保存、筛选、日历跳转、备份恢复、导出、模板、SQLite 或任何数据规则。只有确有展示需要时才允许调整纯展示型 TSX，并同步更新对应 UI 测试。

布局必须继续支持现有三栏模式和单滚动区规则，在 1280×720、1600×900 及 Windows 100%/125% 缩放下可用；中文长标题、长进度、跟进清单和备注不得造成内容遮挡或底部不可达。键盘焦点清晰，减少动态效果模式继续有效。既有状态颜色含义不变。

### V2.7 并行边界

T81 UI 专员（`feat/shadcn-visual-v27`）：允许修改 `src/styles.css`、`src/components/**/*.css`、必要的纯展示型 TSX 及 UI 测试；优先以令牌和样式重构完成，不改 `src/api.ts`、`src/types.ts`、筛选/日期逻辑、Rust、SQLite、数据接口或业务状态流。交付前运行全部 Vitest、V2.6 Edge 界面流程和前端构建，并提供 1280×720、1600×900 的视觉检查结果。

T82 PM：审查改动是否只影响视觉，集成到 `integration/v2`，协调冲突并补充必要验收。

T83 QA（`qa/v2.7`）：固定 PM 指定的集成提交，独立执行功能回归与视觉验收；只修改 `tests/**` 与 `docs/QA-V2.7.md`，不得代改业务代码。

## V2.6 日历操作增强（2026-09-07）

开发前快照固定为本地标签 `v2.5-before-calendar-actions-20260907`，指向提交 `7756ba0`。本轮仍只交付开发/调试版本；用户明确说“确认通过，可以打包”前禁止生成安装包或合入 `main`。

日历不再显示日期格底部按状态汇总的数量与圆点。每条直接展示的事项标题以及悬停浮窗中的每条事项，分别在标题前显示一个与自身状态一致的呼吸灯：待开展灰、进行中绿、已暂停红、已完成橙。最多直显 3 条和“另有 N 项”的规则保持不变；“另有 N 项”只作为超量提示，不再显示状态数量汇总。

每个日期均可通过鼠标悬停或键盘聚焦打开浮窗，包括没有事项的日期。浮窗中的事项标题改为可点击按钮；点击后退出日历、清除会遮挡该事项的搜索及状态/时间/分类条件，回到事项工作台，滚动到对应事项卡、明显标记并自动展开，但不打开右侧修改编辑栏。

浮窗固定提供“新增计划”按钮。点击后退出日历并打开右侧新建事项栏，截止日期默认填入所点击的日历日期；标题、类别、状态、情况、备注、跟进清单等保持现有新事项默认值。该默认日期只是草稿初始值，用户仍可在创建前修改。

### V2.6 并行边界

T71 C（`feat/calendar-actions-v26`）：只修改 `WorkCalendar.tsx`、`calendar.css` 及对应测试，新增可选 `onOpenItem(id)`、`onNewItem(date)` 回调；实现逐事项呼吸灯、移除状态数量汇总、所有日期浮窗、事项按钮与新增计划按钮。不得修改 App、ItemList、ItemEditor、公共 API、类型或 Rust。

T72 B（`feat/calendar-target-v26`）：只修改 `ItemList.tsx`、`ItemEditor.tsx` 及其样式和测试；为 ItemList 增加可选的定位/展开事项能力，为新建编辑器增加可选默认截止日期。不得修改 App、日历、Sidebar、公共 API 或 Rust。

T73 PM：集成后在 App 内连接上述可选接口，管理从日历返回的事项定位状态和新建默认日期；补 App 集成测试。

T74 QA（`qa/v2.6`）：只修改 `tests/**` 与 `docs/QA-V2.6.md`，固定 PM 指定的集成提交测试，不修改业务代码。

## V2.5 日历信息增强（2026-09-07）

开发前快照固定为本地标签 `v2.4-before-calendar-detail-20260907`，指向提交 `9791189`。本轮仍只交付开发/调试版本；用户明确说“确认通过，可以打包”前禁止生成安装包或合入 `main`。

日历日期格直接展示事项标题，每条事项占一行；同日最多直接展示 3 条，超过后显示“另有 N 项”，鼠标悬停或键盘聚焦仍可查看完整标题列表。标题展示不能替代现有状态标记；无日期事项和回收站事项仍不进入日历。

日历顶部增加独立的分类多选筛选，默认显示全部分类，可同时选择多个真实分类及“未分类”。没有选择具体分类等同全部；筛选只影响当前日历，不改变左侧工作台筛选条件。每次重新进入日历恢复“全部分类”。

日期数字字号由 13px 适度提高。日期首行同行展示节日简称；离线展示至少包括元旦、春节、清明、劳动节、母亲节、端午、中秋、国庆。周六、周日以及上述节日当天的日期数字使用协调的红色；不处理每年变化的调休和补班，不联网获取节假日。节日计算需覆盖正常日历导航，不能只写死 2026 年。

事项卡“最新进度”标题与内容从同一行开始，格式视觉上为“最新进度  内容”；长内容及原有换行仍完整展示，宽度不足时自然换行，不截断、不省略。

### V2.5 并行边界

T61 B（`feat/item-card-v25`）：仅修改 `src/components/workbench/item-card.css`、必要时修改 `ItemList.tsx` 及对应测试，实现最新进度标题与正文同行起始；不得改日历、App、Sidebar、公共类型、API 或 Rust。

T62 C（`feat/work-calendar-v25`）：仅修改日历组件、日历工具、日历样式及对应测试；必要时可调整 App 向日历传入的类别数据。实现标题直显、分类多选、节日与周末标红、字号调整；不得改事项卡、编辑器、公共类型、API 或 Rust。

T63 QA（`qa/v2.5`）：只修改 `tests/**` 与 `docs/QA-V2.5.md`，固定 PM 指定的集成提交测试，不修改业务代码。

## V2.4 事项卡层级与工作日历（2026-09-07）

开发前快照固定为本地标签 `v2.3-before-calendar-20260907`，指向提交 `27ae73a`。本轮仍只交付开发/调试版本；用户明确说“确认通过，可以打包”前禁止生成安装包或合入 `main`。

事项卡展开内容中的“情况”必须拥有与最新进度、跟进清单、备注一致的独立浅色底框、内边距和圆角。最新进度、情况、跟进清单、备注四个板块的标题字号和字重统一高于正文一级，正文保持易读且不得截断最新进度全文。本轮只优化信息层级，不进行整体 UI 换肤。

左侧底部增加“工作日历”入口。点击后先处理未保存内容，再把中间工作区切换为只读月历并隐藏右侧编辑栏；每次进入默认定位本机当天所在月份。月历以周一为每周第一天，提供上月、回到今天、下月；显示完整六周网格，相邻月份日期弱化。

月历数据使用应用已加载的全部正常事项，忽略左侧状态、时间、分类和搜索条件，排除回收站与未设截止日期事项。某日有事项时，在日期下方显示与事项状态一致的彩色标记和数量；鼠标悬停或键盘聚焦该日期时显示当日全部事项标题列表。列表仅供查看，不提供新增、编辑、拖动或状态修改。关闭日历或选择“显示全部”和其他筛选入口后返回事项列表。

### V2.4 并行边界

T51 B（`feat/item-card-v24`）：只改 `src/components/workbench/item-card.css` 及必要的 ItemList 测试，补齐情况底框和四个板块的标题/正文层级；不得改 App、Sidebar、公共类型、Rust 或日历代码。

T52 C（`feat/work-calendar`）：只改 `src/App.tsx`、`src/components/workbench/Sidebar.tsx`、Sidebar 测试，可新增 `WorkCalendar.tsx`、日历样式、日历工具与测试；实现只读月历及入口，不得改 ItemList、ItemEditor、公共类型、API 或 Rust。

T53 QA（`qa/v2.4`）：只改 `tests/**` 与 `docs/QA-V2.4.md`，固定 PM 指定的集成提交测试，不修改业务代码。

空间清理约定：保留主工作树以及开发员 B、C 的两个小工作树；已确认并移除其余七个无未提交修改的旧工作树，保留 Git 分支与提交历史。

## V2.3 编辑与筛选功能调整（2026-09-06）

本轮只交付开发/调试版本；用户明确说“确认通过，可以打包”前禁止生成新版安装包或合入 main。本轮暂不进行整体 UI 换肤，不引入外部 UI 工程或 Skill。

日期显示统一为 `X年X月X日`，月份和日期不补零；进度记录、备份记录等需要时间的场景统一为 `X年X月X日 HH:mm`。数据库存储格式不变，各功能统一使用 `src/lib/dateFormat.ts`。

右侧详情编辑栏只保留标题、类别、截止日期、事项情况、备注、跟进清单，以及跟进清单模板的定制和应用；状态与最新进度仍在事项卡操作。编辑栏只允许一个纵向滚动区，顶部保存按钮固定不随内容滚动，保存成功后自动关闭。

点击事项卡“最新进度”标题或正文才显示更新输入框；提交成功只隐藏输入框，不改变事项卡展开状态。左侧增加“显示全部”，点击后退出回收站并清除搜索、状态、时间和分类条件，恢复显示全部正常事项。

### V2.3 并行边界

T41 A（feat/editor-v23）：只改 ItemEditor.tsx、editor.css 及对应测试，实现右栏字段收敛和单滚动区；保留删除入口，不得改 App、ItemList、Sidebar、types/api 或 Rust。

T42 B（feat/item-card-v23）：只改 ItemList.tsx、item-card.css 及对应测试，实现进度输入框独立开关、卡片展开状态保持和日期中文显示；不得改 App、ItemEditor、Sidebar、types/api 或 Rust。

T43 C（feat/filter-reset-v23）：只改 App.tsx、Sidebar.tsx、sidebar.css、BackupPanel.tsx 及对应测试，实现“显示全部”、辅助日期中文显示和必要的左栏紧凑调整；不得改 ItemList、ItemEditor、types/api 或 Rust。

T44 QA（qa/v2.3）：只改 tests/** 和 docs/QA-V2.3.md，固定 PM 指定提交测试，不得修改业务代码。

## V2.2 事项卡、编辑栏与跟进模板冻结（2026-09-06）

本轮仍只交付开发/调试版本；用户明确说“确认通过，可以打包”前禁止生成新版安装包或合入 main。

事项卡默认收起。收起状态完整显示：状态灯、标题、类别与日期小字、最新进度全文、状态控件和最右侧“修改编辑”。最新进度不得截断、省略或限制行数。标题左侧提供展开/收起按钮；展开后显示事项情况、跟进清单和只读备注。标题、事项情况、备注、类别和截止日期不得在事项卡内修改；卡内仅允许提交最新进度、增删勾选跟进项和修改状态。最新进度提交成功后自动收起输入框，失败则保留输入和展开状态。

右侧编辑栏默认完全隐藏，列表占用释放后的宽度；仅点击“新增事项”或事项卡“修改编辑”时打开。编辑栏不提供单独关闭按钮。创建/保存按钮固定在编辑栏顶部，不随表单滚动；新建或修改必须由用户点击该按钮统一提交，成功后自动关闭编辑栏，失败时保持打开并保留全部输入以便重试。现有事项不再依赖普通字段自动保存。打开编辑栏后若未成功提交，不通过切换事项隐式覆盖草稿。

每个真实类别可拥有多个跟进清单模板。模板包含自定义名称和有序文字项，可新增、修改、删除；同一类别内模板名称不得重复，删除类别同时删除其模板。编辑事项时仅展示当前类别的模板；选择模板后把模板文字追加到当前跟进清单，按去除首尾空格后的完整文字跳过重复项，新增项默认未完成。模板后续变化不影响已写入事项的跟进项。未分类不创建模板。

示例：类别“活动”可有模板“健康讲座”（确认场地、邀约客户、准备讲座物料）和“车库开放”（确认活动流程、准备签到物料、安排现场拍摄）。

公共类型与本地接口以 src/types.ts、src/api.ts 为准：FollowUpTemplate、FollowUpTemplateInput，以及 listFollowUpTemplates、createFollowUpTemplate、updateFollowUpTemplate、deleteFollowUpTemplate。后端 Rust serde 使用 camelCase。模板必须进入完整 JSON 备份；新版本应能恢复旧版无模板备份，损坏或无效模板仍须在替换数据前拒绝，恢复失败不得影响原数据。

### V2.2 并行边界

T31 A（feat/follow-up-templates-data）：只改 src-tauri/src/** 与 Rust 测试，实现模板表、校验、CRUD、分类删除联动、备份与向后兼容恢复；不得改前端、types/api、Tauri 配置。

T32 B（feat/collapsible-item-card）：只改 ItemList.tsx、ItemList 测试，并可新增 item-card.css；实现默认收起、完整最新进度、卡内只读边界及进度成功后收起；不得改 App、ItemEditor、editor.css、types/api 或 Rust。

T33 C（feat/explicit-editor-pane）：只改 App.tsx、ItemEditor.tsx、editor.css、styles.css 及对应测试；实现右栏按需显示、顶部固定统一提交、成功关闭、失败保留，并实现跟进模板管理与选择应用界面；不得改 ItemList、types/api 或 Rust。

T34 QA（qa/v2.2）：只改 tests/** 和 docs/QA-V2.2.md，固定 PM 指定提交测试，不得修改业务代码。

## V2.1 界面优化冻结（2026-09-06）

本轮仍只交付开发/调试版本；用户明确说“确认通过，可以打包”前禁止生成新版安装包或合入 main。

窗口在可用高度不足时不得裁掉底部内容。应用外壳占满窗口可用高度，左侧导航、事项列表和右侧详情分别承担自己的纵向滚动；不得依赖页面整体超出窗口后被隐藏。

工作台左侧恢复多维组合筛选：状态、时间、分类每个维度各单选一个条件，三个维度可以同时生效，关键词继续叠加。选择某维度的“全部”只清除该维度。时间选项为全部时间、今天、本周、下周、本月、下月、历史；左侧删除已逾期。事项本身的逾期提示规则保持不变。

状态、时间、分类三个分组标题使用协调的浅色底框和较大字体，各自独立展开/收起。首次使用默认仅展开状态，时间和分类收起；后续在本机记住用户的展开状态。

事项卡采用紧凑布局。标题栏同一行依次为状态呼吸圆点、较大标题、状态控件和最右侧“修改编辑”；标题下的小字只显示“类别 · 截止日期”。标题区域与内容区域之间使用分割线，不增加空白行。卡片内不得再次显示标题输入框、类别字段或截止日期字段。情况仅以一行摘要展示，标题和情况必须进入详情修改。列表内仍可操作最新进度、备注、跟进清单和状态；删除“一键完成”。进度、备注默认各显示一行，点击后展开编辑；跟进默认最多显示三条，超出后提供展开/收起。

状态圆点颜色：doing 绿色、todo 灰色、paused 红色、done 橙色；采用轻微呼吸动画，并在系统设置减少动画时停止动画。

### V2.1 并行边界

T21 A（feat/window-scroll-fix）：只改 src/styles.css，修复窗口底部裁切和三栏滚动，不改变业务组件。

T22 B（feat/compact-item-card）：只改 ItemList.tsx、ItemList 测试和 editor.css，实现紧凑事项卡、只读标题/情况、标题同行状态与编辑按钮、状态灯、按需展开编辑；不得改 App/Sidebar/filters/Rust。

T23 C（feat/multidim-sidebar）：只改 App.tsx、Sidebar.tsx、新增 sidebar.css、filters 及其测试，实现多维组合、时间项调整和可记忆折叠分组；不得改 ItemList/ItemEditor/Rust/全局 styles。

T24 QA（qa/v2.1）：只改 tests/** 和 docs/QA-V2.1.md，固定 PM 指定提交后测试，不得修改业务代码。

## V2 功能冻结（2026-09-06）

V2 只交付开发/调试版本，用户明确说“确认通过，可以打包”前禁止生成新版安装包。

工作台左侧顺序为“状态、时间、分类”。状态、时间、分类是单维度筛选：任意时刻只生效其中一个具体条件，选择另一个维度时清除前一个维度；关键词搜索始终可以叠加。时间选项为全部时间、今天、本周、下周、本月、下月、历史、逾期；删除未设日期和自定义日期。“历史”是截止日期早于本机今天的所有事项，不限制状态；“逾期”仍只包含截止日期早于今天且状态为 todo/doing 的事项。

事项卡直接展示标题、工作内容（界面称“情况”）、最新进度、跟进清单、备注、状态、分类和截止日期。可在卡片内提交新进度、编辑备注、增删改勾跟进项、修改状态和一键完成；进度必须新增历史记录，跟进勾选不得自动改状态。类别和截止日期只展示，必须点击“修改编辑”进入现有详情编辑器修改。内联写入成功通过 onChanged(WorkItem) 回传根组件，失败通过 onError(string) 展示且保留输入。

现有选择位置导出 JSON 和恢复功能保留。新增 quickBackup，无文件对话框，写入 app data dir 下的“备份”目录。当天首份名称为“工作备份文件YYYYMMDD.json”；同名已存在时增加不会覆盖旧文件的序号后缀。返回 QuickBackupResult（包含 path 与 BackupInfo 字段）。

新增 exportWorkItems(input)，由界面选择 TXT 目标路径。statuses、dateFilters、categoryIds 均支持多选；空数组表示该维度全选，all 与其他时间项同时出现时按全部时间处理，重叠结果去重。categoryIds 的 null 表示未分类。只导出未删除事项。按当前类别顺序分组，未分类最后；组内截止日期正序，无日期最后。每项格式为“序号、YYYY年M月D日，事项标题，事项最新进度；”，组内末项用句号；无日期写“未设日期”，无进度写“暂无最新进度”。文件编码 UTF-8，类别标题使用中文序号。导出默认建议文件名“工作事项汇总YYYYMMDD.txt”。

V2 新接口以 src/types.ts 和 src/api.ts 为准：quickBackup()、exportWorkItems(input)。后端 Rust serde 继续使用 camelCase。

## V2 并行边界

T11 A（feat/quick-backup-export）：只改 src-tauri/src/**、必要的 Cargo 文件与 Rust 测试，实现一键备份及 TXT 导出。

T12 B（feat/inline-item-card）：只改 ItemList.tsx、ItemList 测试、ItemEditor 及 editor.css；实现事项卡展示和内联编辑。不得修改 App、types/api、全局 styles 或 Rust。ItemList 使用基线已提供的 onChanged/onError。

T13 C（feat/workbench-v2）：只改 App.tsx、styles.css、Sidebar、BackupPanel、新增 ExportPanel、filters 及相应测试；实现单维筛选、历史、界面顺序、一键备份入口和导出界面。不得修改 ItemList、ItemEditor、types/api 或 Rust。

T14 QA（qa/v2）：只改 tests/**、测试配置和 docs/QA-V2.md；不得修改业务实现。固定 PM 指定提交后测试。

PM：维护公共契约、处理集成冲突、启动开发版、最终验收；本轮暂不打包。

## 功能冻结
Windows 本地单用户工作备忘录。预设类别：推广、包装、活动、拓展、方案、其他，另有虚拟未分类（categoryId=null）。分类可增改排序和删除，删除分类关联事项转未分类。
截止日期仅 YYYY-MM-DD，以本机日期判断。状态 todo/doing/done/paused；仅 todo 和 doing 可逾期。
筛选：全部、今天、本周、下周、本月、下月、逾期、未设日期、自定义；周一为周始、边界含当日。默认到期日升序，空日期最后，同日期按创建时间降序。
搜索标题、内容、最新进度、跟进文字、备注。跟进勾选不自动修改事项状态。
新事项标题必填，创建按钮首次保存；普通字段自动保存。更新进度单独提交，保留历史。
删除仅软删除，可恢复。备份为带版本的 JSON，包括类别、所有事项（含回收站）、跟进和进度。恢复必须先完整校验、再安全备份当前数据库内容、最后事务替换；失败无数据变动。

## 数据与本地接口
src/types.ts 和 src/api.ts 为固定接口。Rust serde 使用 camelCase。所有时间戳为 RFC3339；progress 数组最新在前。listItems 包含回收站条目，UI 分离显示。categoryId 可空。后端返回给用户可读的中文错误。
ItemInput 不包括 progress，更新普通字段不能覆盖历史。updateItem 后端保留 id、createdAt、progress、deletedAt。
reorderCategories 必须提供全部现有类别 ID 且各一次。重复或空类别名称不允许。
exportBackup/inspectBackup 返回 BackupInfo；restoreBackup 返回安全备份路径和数量。
数据库存 AppHandle.path().app_data_dir()。支持 WORK_MEMO_DATA_DIR 环境变量作为明确测试专用覆盖，默认不设置。单实例插件避免同时开启两个程序写入。

## UI 集成边界
开发员 B 提供 src/components/ItemEditor.tsx 默认 forwardRef 组件，类型 ItemEditorProps / EditorHandle 见 types.ts。prepareLeave() 完成自动保存；未创建草稿或未提交进度需询问保留/放弃（至少确认放弃后才离开），失败返回 false。组件内部调用 api，成功 onSaved(WorkItem)；删除成功 onDeleted(id)。item=null 表示新建。根组件通过 key=item.id 或 new 来区分编辑对象，先 await prepareLeave 再换 key。
父组件 onSaved 只更新条目数据，不能因每次保存重新挂载编辑器；创建完成后需安全切换到其 id。
开发员 C 提供 src/App.tsx、src/styles.css、src/components/workbench/**、src/lib/filters.ts。C 不实现编辑器。共享编辑器 CSS 由 B 在独立 editor.css 内命名空间 .item-editor 下定义。C 给右栏合理宽度（约 430px，可伸缩）；1060px 最小窗口也可使用。语义化 label/button，方便键盘和测试。
所有危险操作用中文明确确认；桌面确认和选择文件使用 @tauri-apps/plugin-dialog。无浏览器生产数据后备。

## 分派
T01 A：src-tauri/src/** 实现、Cargo.toml/Cargo.lock 必要依赖、后端测试。不要改前端、Tauri 配置或图标。交付独立 Rust 数据测试（tempfile），能运行 cargo test。
T02 B：src/components/ItemEditor.tsx、editor.css、同目录编辑器辅助文件及测试。不要改 App、types、api、package 或全局 styles。
T03 C：src/App.tsx、src/styles.css、src/components/workbench/**、src/lib/** 及筛选测试。负责类别管理和备份/回收站界面，关窗 prepareLeave 防丢。不要改编辑器、types/api/package 或 Rust。
T04 QA：tests/**、测试配置文件及 docs/QA.md；不得更改业务实现。先编写用例，待 PM 指定集成提交后执行。可使用 Tauri mockIPC 做浏览器 UI 测试，但必须明确与真实 SQLite/桌面检查区分；用故障注入测试保存失败/恢复失败。测试结果写明版本。
PM：公共基线、依赖、图标、打包、接口协调、集成、使用说明、最终验收。

## 交付流程
开发员本分支自测并提交，报告 hash、修改范围、测试结果。PM 合入 integration/v1，QA 固定该提交测试。缺陷退回原开发员，修复再集成复测。最终接受的集成提交快进 main 后构建安装包。禁止用“只写完代码”代替验证。
